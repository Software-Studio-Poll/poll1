<%= form_with(model: userchoice, local: true) do |form| %>
  <% if userchoice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(userchoice.errors.count, "error") %> prohibited this userchoice from being saved:</h2>

      <ul>
        <% userchoice.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :answerchoice_id %>
    <%= form.number_field :answerchoice_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit "✔", class: "btn btn-primary" %>
  </div>
<% end %>
